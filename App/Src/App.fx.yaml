App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =//Criando as variaveis globais
        Set(
            gblTeste1,
            1
        );
        Set(
            gblTeste2,
            2
        );
        Set(
            gblTeste3,
            3
        )
    StartScreen: =AppScreen2
    Theme: =OrangeTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

